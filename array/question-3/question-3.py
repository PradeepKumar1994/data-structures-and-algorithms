class Replacing:    def __init__(self, string_):        self.string_ = string_    def replacing(self, str_):        try:            temp = int(str_)            if(temp % temp)    def replace_space(self):        string_array = self.string_.split('"')        print("Array: ",string_array)        for i in string_array:            resultant = replacing(i)            result.append(resultant)